version: '3.9'
services:
  web:
    build: .
    ports: ["5001:5000"]
    env_file: .env
    depends_on: [mongo]
  mongo:
    image: mongo:7
    ports: ["27018:27017"]
    volumes:
      - mongo_data:/data/db
volumes:
  mongo_data:
